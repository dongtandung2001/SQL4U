import React from "react";

const DashboardSideBar = ({ icon }) => {
  return (
    <div className="dashboardSideBarContent">
      <div>{icon}</div>
    </div>
  );
};

export default DashboardSideBar;
